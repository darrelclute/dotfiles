[Unit]
Description=Music Player Daemon
IgnoreOnIsolate=true
Wants=cli.target
After=environment.target pulseaudio.service

[Service]
ExecStart=/usr/bin/mpd --no-daemon

[Install]
WantedBy=default.target
