# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTY
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard
#

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:Source Code Pro for Powerline Medium 7
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Leave full screen when a popup is presented, so that Pinentry is not missed.
popup_during_fullscreen leave_fullscreen

# Switching to same workspace twice will return you to previous workspace.
workspace_auto_back_and_forth yes

# use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $up k
set $down j
set $left h
set $right l

# Aliases for modifiers
set $defmod Mod4
set $super Mod4
set $alt Mod1

# Aliases for workspaces
set $ws1 workspace 1: web
set $ws2 workspace 2: terminal
set $ws3 workspace 3
set $ws4 workspace 4
set $ws5 workspace 5
set $ws6 workspace 6
set $ws7 workspace 7
set $ws8 workspace 8
set $ws9 workspace 9
set $ws10 workspace 10
set $ws11 workspace 11
set $ws12 workspace 12

# use Mouse+$defmod to drag floating windows to their wanted position
floating_modifier $defmod

# start a terminal
bindsym $defmod+Return exec konsole --fullscreen

# start chrome
#bindsym $defmod+

# kill focused window
bindsym $defmod+Shift+q kill

# start dmenu (a program launcher)
bindsym $defmod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $defmod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $defmod+$left focus left
bindsym $defmod+$down focus down
bindsym $defmod+$up focus up
bindsym $defmod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $defmod+Left focus left
bindsym $defmod+Down focus down
bindsym $defmod+Up focus up
bindsym $defmod+Right focus right

# move focused window
bindsym $defmod+Shift+$left move left
bindsym $defmod+Shift+$down move down
bindsym $defmod+Shift+$up move up
bindsym $defmod+Shift+$right move right

# alternatively, you can use the cursor keys:
bindsym $defmod+Shift+Left move left
bindsym $defmod+Shift+Down move down
bindsym $defmod+Shift+Up move up
bindsym $defmod+Shift+Right move right

# split in horizontal orientation
bindsym $defmod+minus split h

# split in vertical orientation
bindsym $defmod+bar split v

# enter fullscreen mode for the focused container
bindsym $defmod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $defmod+s layout stacking
bindsym $defmod+w layout tabbed
bindsym $defmod+e layout toggle split

# toggle tiling / floating
bindsym $defmod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $defmod+space focus mode_toggle

# focus the parent container
bindsym $defmod+a focus parent

# focus the child container
#bindsym $defmod+d focus child

# move the currently focused window to the scratchpad
#bindsym $defmod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
#bindsym $defmod+minus scratchpad show

# switch to workspace
bindsym Control+F1 $ws1
bindsym Control+F2 $ws2
bindsym Control+F3 $ws3
bindsym Control+F4 $ws4
bindsym Control+F5 $ws5
bindsym Control+F6 $ws6
bindsym Control+F7 $ws7
bindsym Control+F8 $ws8
bindsym Control+F9 $ws9
bindsym Control+F10 $ws10
bindsym Control+F11 $ws11
bindsym Control+F12 $ws12

# move focused container to workspace
bindsym Control+Shift+F1 move container to $ws1
bindsym Control+Shift+F2 move container to $ws2
bindsym Control+Shift+F3 move container to $ws3
bindsym Control+Shift+F4 move container to $ws4
bindsym Control+Shift+F5 move container to $ws5
bindsym Control+Shift+F6 move container to $ws6
bindsym Control+Shift+F7 move container to $ws7
bindsym Control+Shift+F8 move container to $ws8
bindsym Control+Shift+F9 move container to $ws9
bindsym Control+Shift+F10 move container to $ws10
bindsym Control+Shift+F11 move container to $ws11
bindsym Control+Shift+F12 move container to $ws12

# reload the configuration file
bindsym $defmod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $defmod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $defmod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# lock i3
bindsym $alt+Control+l exec --no-startup-id xautolock -locknow

# mpd controls
# play/pause output
bindsym XF86AudioPlay exec --no-startup-id mpc toggle
bindsym XF86AudioPrev exec --no-startup-id mpc prev
bindsym XF86AudioNext exec --no-startup-id mpc next
bindsym XF86AudioStop exec --no-startup-id mpc stop

# Control volume (mute/unmute)
bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 3%-
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 3%+

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left       resize shrink width 10 px or 10 ppt
        bindsym $down       resize grow height 10 px or 10 ppt
        bindsym $up         resize shrink height 10 px or 10 ppt
        bindsym $right      resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $defmod+r mode "resize"

exec --no-startup-id xset dpms 900
exec --no-startup-id xss-lock -- i3lock -c 000000 
exec --no-startup-id xautolock -time 10 -locker "i3lock -c 000000"
exec --no-startup-id xrdb merge ~/.Xresources
exec --no-startup-id xmodmap ~/.Xmodmap
exec pasystray

bar {
     #status_command python2 ~/bin/powerline-i3.py
     status_command i3status
     font pango:Source Code Pro for Powerline Medium 7
     mode dock
     position bottom
     tray_output primary
     strip_workspace_numbers yes
}
